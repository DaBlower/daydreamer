[gd_scene load_steps=5 format=3 uid="uid://d126lgsapnqh2"]

[ext_resource type="Texture2D" uid="uid://bhrd8duiw703t" path="res://assets/player.png" id="1_xrxa3"]
[ext_resource type="Texture2D" uid="uid://dfyiy4bw0shpk" path="res://assets/candle/gradient.png" id="2_k5rr8"]

[sub_resource type="GDScript" id="GDScript_g2els"]
script/source = "extends CharacterBody2D

const SPEED = 300.0
const JUMP_VELOCITY = -800.0
var light_radius: float = 0.1
var max_radius: float = 0.2
var min_radius: float = 0.0

@onready var point_light_2d: PointLight2D = $PointLight2D

func _physics_process(delta: float) -> void:
	# Add the gravity.
	if not is_on_floor():
		velocity += get_gravity() * delta

	# Handle jump.
	if Input.is_action_pressed(\"ui_up\") and is_on_floor():
		velocity.y = JUMP_VELOCITY
	
	# handle candle
	if Input.is_action_just_pressed(\"increase_candle\"):
		light_radius += 0.5
	
	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction := Input.get_axis(\"ui_left\", \"ui_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)

	move_and_slide()

func _process(delta: float) -> void:
	light_radius -= 0.05 * delta
	light_radius = clamp(light_radius, min_radius, max_radius)
	point_light_2d.texture_scale = light_radius
	
	
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_qhqgy"]
size = Vector2(128, 128)

[node name="Player" type="CharacterBody2D"]
script = SubResource("GDScript_g2els")

[node name="Sprite2D" type="Sprite2D" parent="."]
position = Vector2(-2.0000014, 18.499998)
scale = Vector2(2.0399997, 3.11)
texture = ExtResource("1_xrxa3")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
scale = Vector2(1, 2)
shape = SubResource("RectangleShape2D_qhqgy")

[node name="PointLight2D" type="PointLight2D" parent="."]
texture = ExtResource("2_k5rr8")
texture_scale = 0.01

[node name="Camera2D" type="Camera2D" parent="."]
position = Vector2(2, -1)
limit_left = 0
drag_horizontal_enabled = true
drag_vertical_enabled = true
